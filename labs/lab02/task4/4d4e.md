d) for delay = 1, the 3 implementations produced were all identical, producing correct results. for
    delay = 2, the 3 implementations diverged by a lot, with each implementation varying. For delay = 3, all implementations failed to track the correct value.

e) This simulation teaches that the delay value alone wont tell about if a design will behave
    correctly. A gate that works perfectly under one input rate can fail completely once inputs move faster than its own delay.